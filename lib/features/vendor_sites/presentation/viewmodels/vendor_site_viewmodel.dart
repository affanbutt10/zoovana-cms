import 'package:get/get.dart';

/// Placeholder ViewModel for the Vendor Sites list screen.
///
/// Extends [GetxController] as required by the GetX architecture.
/// Business logic and observables will be added in a future task when
/// vendor site management (listing, create, update, delete) is implemented.
class VendorSiteViewModel extends GetxController {
  // Placeholder — no logic needed yet.
  // Future observables (e.g., vendorSites list, isLoading, errorMessage) will be added here.
}
